---
name: notion-database-management
description: |
  Low-level Notion API operations via the `ntn` CLI — now consolidated into
  notion-workbook. This skill redirects there.
---

# Notion Database Management

All Notion API mechanics — auth, query patterns, page CRUD, property types, error recovery — are in the shared workbook:

**→ Read `skill://notion-workbook` for the full reference.**

This includes:
- Installing and authenticating `ntn`
- Database IDs (Epics, Tasks, Insights)
- Query patterns (datasources vs raw API)
- Page creation, reading, updating
- Property type JSON shapes
- Error recovery flows
