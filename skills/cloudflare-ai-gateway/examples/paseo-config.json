{
  "agents": {
    "providers": {
      "omp": {
        "command": ["~/.paseo/omp-yolo.sh"],
        "models": [{
          "id": "cloudflare-gateway/dynamic/fallback2",
          "label": "Muse Spark 1.3 → DeepSeek V4 Flash (via Cloudflare)",
          "isDefault": true
        }],
        "enabled": true
      }
    }
  }
}